name: build

on:
  push:
  pull_request:

jobs:
  ui:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun install --frozen-lockfile
      - run: bun run build
      - name: built tree present
        run: |
          test -f ../pb_public/subs/index.html
          test -f ../pb_public/subs/skadi-mark.svg
          ls ../pb_public/subs/assets | grep -q .
      - uses: actions/upload-artifact@v4
        with:
          name: subs-ui
          path: pb_public/subs
